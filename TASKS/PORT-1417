PORT-1417

Бэк. - 5 часов.
	Создаем группу роутов shipments. В ней роут списка "/" и подгруппу api, в которой роут ::post "/".
	Создаем контроллер ShipmentRequestsController.
	Создаем конфиг shipments_group.php прописываем связи.
	Создаем модель ShipmentsGroup.php
	Страница списка.
		Метод list():
			Читаем из запроса данные по каждому фильтру. Передаем их в шаблон shipments.groups.list вместе с языком, метаданными и хлебными крошками.
		Метод apiGetList():
			1. Создаем app/ModelQueries/ShipmentsGroupQuery:access() по аналогии с app/ModelQueries/ShipmentAppealQuery:access().
			2. Получаем группы ЗНО с учетом доступов используя модель ShipmentsGroup. 
			3. Подготавливаем коллекцию из групп ЗНО со всеми требуемыми полями списка:
			 - Заводим признак `rowspan` для каждой группы. В каждой группе коллекция ЗНО, величина которой совпадает со значением в `rowspan`.  
			 - Внутри каждой ЗНО будут признаки dealer_rowspan, entity_rowspan. Они будут равны общим значениям дилера и юр. лица соответственно. 
				Основная идея: при итерации отдельной ЗНО в группе, пробрасывать в замыкание все ЗНО данный группы. Далее ведем поиск по dealer_id и количество найденных записей записываем в dealer_rowspan.
			4. Передаем все в ответе вместе с метаданными и данными фильтров.
Фронт по аналогии с другими списками с ListController - 4 часа.
Итого 9 часов + 2 часа на риски.

Прописать sql запрос на создание shipments_groups
Прописать sql запрос на добавление поля
Переписать сбор коллекции по паттерну группаЗНО->диллер->юр.лицо->зно.






