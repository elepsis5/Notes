PORT-1307

Бэк.
При преключении будут записываться 1 или 3 куки. Зона и Зона, Дилер, Юрлицо соответственно.
В DTO app\Classes\UserData добавляется новое поле dealers. Соответственно обновляются методы construct() и check().
В модель app/Models/Person.php добавляется новая связь dealers.
В app/EloquentModels/User::initialSetup()
	- получаем всех дилеров. В БД нужно создать сводную таблицу users<->dealers. ***нужно ли новое поле для зоны в таблице дилеров? обратиться к Юре***
	- проверяем какие куки имеются.
	- Если есть только Зона (или нет полного комплекта Зона, Дилер, Юрлицо), берется логика, как если бы не был найден дилер:
	***image***
	- Если есть все 3 куки, получаем дилера и юрлицо по ним. Проверяем соответстввие дилера зоне и соответствие юрлица дилеру. Если надено НЕсоответствие ***обратиться к Юре***. 
	Записываем полученного дилера $this->data->dealer (проверить все обращения к $user->dealer, их требуется изменить на $user->data->dealer)
	В data->legalEntities храним все юрлица всех связанных dealers. В data->selectedLegalEntity выбранное юрлицо.
В CheckZone убрать условие $user->hasPermission('change_zone') и в целом адаптировать под User DTO. Проверить где еще может быть обращение к роли change_zone.

Фронт.
Список выводится на основе вроженного списка <ul> с использованием bootstrap dropdowns. По нажатию на один из вариантов записать куки (1 или 3), и перезагрузить страницу.



нужно ли новое поле для зоны в таблице дилеров?
***да, давай сделаем - проработать как и на каком этапе новое поле будет заполняться и актуализироваться***
	- актуализироваться будет на этапе синхронизации customers на основе сводной таблицы dl_dealers_sales_types.

Вопросы:
"Список выводится на основе вложенного списка <ul> с использованием bootstrap dropdowns"
***есть пример вёрстки как примерно это будет выглядеть? Не нашёл в bootstrap dropdowns примеры с иерархической структурой***
	- https://stackoverflow.com/questions/51871908/bootstrap-4-nested-dropdown
"В БД нужно создать сводную таблицу users<->dealers"
***опиши это подробнее***
	- создаем новую таблицу us_users_dilers с полями user_id и dealer_id и заполняем ее на основе данных из таблицы users.
"Если есть все 3 куки, получаем дилера и юрлицо по ним"
***куки получаются прямо в initialSetup() или где-то раньше? (контейнеры?)***
	- в контейнере куки и так привязываются. в initialSetup() мы получаем зону из контейнера, а остальные получаем через хелпер getCookie().
"В app/EloquentModels/User::initialSetup()"
***будет добавляться свойство для хранения всех привязанных дилеров? или каждый раз будет тянуться через связь?***
	- записываем в $user->data.
"Если есть только Зона (или нет полного комплекта Зона, Дилер, Юрлицо), берется логика, как если бы не был найден дилер"
***я бы описал отдельную логику, например в таком случае в legalEntities будет пустая, почему бы не хранить там все юр.лица дилеров из указанной зоны? Можем обсудить это***
	- 
***описать подробно действия при получении невалидных данных из кук. Где и как будут валидироваться?***
	- по зоне определяем всех дилеров. Сравниваем с дилером из кук. Если есть совпадение ищем юрлица и аналогично сравниваем по совпадению со значением из кук. если вся цепочка пройдена, то данные кук считаются валидными.


***+ описать подробно ситуацию когда выбрано "ALL"***
	- при выборе ALL все привязки к юрлицу и дилеру сбрасываются. В $user->data выставляются пустые значения:




зону получаем:
если юзер:
	1. если есть привязанный дилер:
		а) из кук, если они есть
		б) если кук нет, то из дилера.
	2. если дилера нет, то из юзера через федеральный регион.
если не юзер:
	через определение страны


ИТОГО:

Sync.


Бэк.
	В DTO app\Classes\UserData добавляется новое поле dealers. Соответственно обновляются методы construct() и check(). - 15 мин
	В модель app/Models/Person.php добавляется новая связь dealers. - 15 мин
	В app/Http/Middleware/CheckZone.php валидация кук и запись в контейнер. - 45 мин
	В app/EloquentModels/User::initialSetup() - на основе данных по зоне, дилеру и юрлицу из контейнера заполняем свойства 
	 - data->dealer;
	 - data->dealers;
	 - data->legalEntities;
	 - data->selectedLegalEntity;
	
	- получаем всех дилеров. В БД нужно создать сводную таблицу users<->dealers. ***нужно ли новое поле для зоны в таблице дилеров? обратиться к Юре***
	- проверяем какие куки имеются.
	- Если есть только Зона (или нет полного комплекта Зона, Дилер, Юрлицо), берется логика, как если бы не был найден дилер:
	***image***
	- Если есть все 3 куки, получаем дилера и юрлицо по ним. Проверяем соответстввие дилера зоне и соответствие юрлица дилеру. Если надено НЕсоответствие ***обратиться к Юре***. 
	Записываем полученного дилера $this->data->dealer (проверить все обращения к $user->dealer, их требуется изменить на $user->data->dealer)
	В data->legalEntities храним все юрлица всех связанных dealers. В data->selectedLegalEntity выбранное юрлицо.
В CheckZone убрать условие $user->hasPermission('change_zone') и в целом адаптировать под User DTO. Проверить где еще может быть обращение к роли change_zone.
