PORT-1418

Используем библиотеку PhpOffice.
В контроллере ShipmentRequestsController создаем метод createExcel(), который возвращает сформированный файл.
Внутри метода Получаем группу ЗНО. В цикле проходимся по ЗНО в группе и по товарам в них, заполняя объект Spreadsheet() данными.

Время 4 часа + 1 час на риски