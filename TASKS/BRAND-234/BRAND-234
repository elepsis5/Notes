BRAND-234

Создаем роут, метод home() в контроллере MainController (путь уточнить).

1. Слайдер
	js. За основу берем bootstrap слайдер.
	медиа. Храним по аналогии брендов в директории public.
	текст. Хранится в таблице translate. Берем в зависимости от языка.

2. Top Category
	идентификаторы категорий храним в константе. Получаем по ним категории передаем в шаблон main.blade.php
	медиа. Храним по аналогии брендов в директории public.

3. Deal
	медиа. Храним по аналогии брендов в директории public.
	текст. Хранится в таблице translate. Берем в зависимости от языка.
	попап "Форма обратной связи". Берем bootstrap попап.

4. Products
	Категории. Составлем запрос к таблице pr_filters_data. Схлопываем по SECTION_ID передаем в шаблон.
	Товары. Логика состоит в том, чтобы получить все товары hurakan (идентификаторы) и составить запрос к таблице order_shipped.

5. Выделенный текст
	медиа. Храним по аналогии брендов в директории public.
	текст. Хранится в таблице translate. Берем в зависимости от языка.

Верстку копируем из Демо2. (кроме слайдера)